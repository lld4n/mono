import React from 'react';

export default function joinRoomLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <section>
      {children}
      error.layout
    </section>
  );
}
