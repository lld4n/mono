export interface InternationalizationType {
  root: {
    rules: string;
    google: string;
    join: string;
    create: string;
    open: string;
    exit: string;
  };
  room: {
    title: string;
    subtitle: string;
    character: string;
    input: string;
  };
  copy: string;
}
