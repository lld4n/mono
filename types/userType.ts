export type userType = {
  displayName: string;
  email: string;
  photoUrl: string | null;
  themeIndex: number;
  langIndex: number;
  // characterName?: string | null;
  // characterUrlImage?: string | null;
  // stats?: string[]; // пока не уверен
  // playedGames?: string[]; // пока не уверен
};
